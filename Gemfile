source "https://rubygems.org"

ruby "3.3.5"

# Your server uses only Ruby stdlib (webrick, net/http, net/smtp, json, csv, etc.)
# webrick was removed from the default gems in Ruby 3.0+, so we pin it here.
gem "webrick", "~> 1.8"
